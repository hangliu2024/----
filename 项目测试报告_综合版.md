# 资产管理系统 - 综合测试报告

**测试日期：** 2026-05-08  
**测试范围：** 后端代码审查、前端模板审查、安全检查、用户体验评估

---

## 一、严重安全问题 🔴

### 1.1 SECRET_KEY 使用硬编码默认值
- **文件：** `config.py:4`
- **问题：** `SECRET_KEY` 默认使用 `'asset-mgmt-default-secret-key-change-in-production-2026'`，虽然有 warning 提示，但如果环境变量未设置，攻击者可直接利用已知密钥伪造 session、篡改 cookie。
- **风险等级：** 🔴 严重
- **建议：** 生产环境必须强制要求设置环境变量，未设置时应拒绝启动。

### 1.2 AI API 密钥硬编码在配置文件中
- **文件：** `config.py:58`
- **问题：** `AI_API_KEYS = ['asset-ai-api-key-2024']` 直接硬编码在源码中，如果代码泄露则密钥泄露。
- **风险等级：** 🔴 严重
- **建议：** 从环境变量读取，逗号分隔支持多个 key。

### 1.3 密码策略不一致
- **文件：** `app/routes/admin.py:97` vs `app/forms.py:11-26`
- **问题：** 
  - `admin.py` 创建用户时只要求密码 ≥ 6 位
  - `forms.py` 注册表单要求 ≥ 8 位 + 大写 + 小写 + 数字
  - 管理员重置密码无任何复杂度校验
- **风险等级：** 🔴 高
- **建议：** 统一密码策略，管理员创建/重置密码也应遵循相同规则。

### 1.4 SQL 注入风险（间接）
- **文件：** `app/decorators.py:107-114`
- **问题：** `get_user_permissions()` 使用 `text()` 原始 SQL，虽然使用了参数化查询 `:uid`，但整个项目中多处使用 `.ilike(f'%{search}%')` 的方式拼接搜索条件。虽然 SQLAlchemy ORM 的 `ilike` 是安全的，但建议全局检查确保没有直接字符串拼接 SQL 的情况。
- **风险等级：** 🟡 中（当前 ORM 方式安全，但需警惕）

### 1.5 登录失败锁定基于 IP 内存存储
- **文件：** `app/routes/auth.py:16-17`
- **问题：** 
  - `_login_failures` 和 `_active_sessions` 都存储在内存中
  - 服务重启后数据丢失，锁定机制失效
  - 多 worker 进程（如 gunicorn 多进程）下无法共享，攻击者可通过不同 worker 绕过锁定
- **风险等级：** 🔴 高
- **建议：** 使用 Redis 等外部存储。

### 1.6 开放重定向防护不完整
- **文件：** `app/routes/auth.py:96-100`
- **问题：** `next_page.startswith('/')` 的检查可以被 `//evil.com` 绕过（双重斜杠开头也以 `/` 开头）。
- **风险等级：** 🟡 中
- **建议：** 使用 `urlparse` 验证是否为相对路径，排除 `//` 开头的情况。

---

## 二、后端逻辑问题 🟠

### 2.1 Dashboard 缓存不区分用户和部门权限
- **文件：** `app/routes/assets.py:18-49`
- **问题：** 
  - Dashboard 使用全局内存缓存 `_dashboard_cache`，所有用户看到相同的统计数据
  - 有部门权限限制的用户应该只能看到自己部门的数据统计，但实际看到全量数据
  - 缓存 TTL 5 分钟，数据更新后不能及时反映
- **风险等级：** 🟠 高（数据权限泄露）

### 2.2 工控机查询缺少部门权限过滤
- **文件：** `app/routes/assets.py:87-109`
- **问题：** `industrial_computers()` 路由虽然使用了 `@department_permission_required` 装饰器检查用户是否有部门权限，但查询时没有根据用户部门过滤数据。所有用户都能看到全部工控机数据。
- **风险等级：** 🟠 高
- **对比：** `office_computers()` 正确实现了部门过滤（第62-66行）

### 2.3 导出功能缺少权限控制
- **文件：** `app/routes/assets.py:220-336`, `555-632`
- **问题：** 
  - `export_computers()` 和 `export_industrial_computers()` 只使用了 `@login_required`
  - 缺少 `@permission_required` 装饰器，任何登录用户都能导出数据
  - 工控机导出没有部门权限过滤
- **风险等级：** 🟠 高

### 2.4 导入功能装饰器顺序问题
- **文件：** `app/routes/assets.py:339-342`
- **问题：** `@login_required` 和 `@department_permission_required` 的装饰器顺序有问题。`@login_required` 应该在最外层（最后写），但实际上 `@login_required` 在 `@department_permission_required` 之前。虽然 `department_permission_required` 内部也检查了认证状态，但这不是最佳实践。
- **风险等级：** 🟡 低

### 2.5 模板下载缺少权限检查
- **文件：** `app/routes/assets.py:114-217`, `471-552`
- **问题：** 模板下载路由只检查了 `@login_required`，没有检查模块权限。
- **风险等级：** 🟢 低

### 2.6 管理员删除用户未处理关联数据
- **文件：** `app/routes/admin.py:258-276`
- **问题：** 删除用户时直接 `db.session.delete(user)`，没有处理关联的：
  - 登录日志（`LoginLog` 外键引用 `user.id`）
  - 操作日志（`OperationLog` 外键引用 `user.id`）
  - 稽查任务（`AuditTask` 外键引用 `user.id`）
  - 角色关联（`SysUserRole`）
  - 会导致数据库外键约束错误
- **风险等级：** 🔴 高

### 2.7 操作日志记录在同一事务中
- **文件：** `app/routes/admin.py:424-441`
- **问题：** `log_operation()` 函数内部调用 `db.session.commit()`，与业务操作在同一个 session 中。如果日志记录失败会触发 rollback，可能影响业务数据的完整性。反之，业务操作的 commit 和日志的 commit 之间没有事务隔离。
- **风险等级：** 🟡 中

### 2.8 登录时 update_at 不会自动更新
- **文件：** `app/models.py:47`
- **问题：** `updated_at = db.Column(db.DateTime, default=datetime.utcnow)` 只设置了创建时的默认值，没有 `onupdate` 参数。编辑用户信息时需要手动设置 `user.updated_at = datetime.utcnow()`，容易遗漏。
- **风险等级：** 🟢 低

### 2.9 datetime.utcnow() 已弃用
- **文件：** 整个项目
- **问题：** Python 3.12+ 中 `datetime.utcnow()` 已弃用，应使用 `datetime.now(timezone.utc)` 代替。
- **风险等级：** 🟡 中

### 2.10 DepartmentForm 的 parent_id 选择器缺少 choices 初始化
- **文件：** `app/forms.py:50`
- **问题：** `parent_id = SelectField('上级部门', choices=[], coerce=int)` choices 为空，如果在路由中忘记填充 choices，提交表单会报错。
- **风险等级：** 🟢 低

---

## 三、数据库设计问题 🟠

### 3.1 Personnel 模型字段长度过于精确
- **文件：** `app/models.py:134-253`
- **问题：** 人员信息表的字段长度设置非常精确（如 `company_dept = db.String(21)`, `dept_full_name = db.String(78)`），这种做法：
  - 容易因数据源格式变化导致截断
  - 导入数据时频繁触发数据截断错误
  - 不利于系统扩展
- **建议：** 适当放宽字段长度，或使用 `Text` 类型。

### 3.2 日期字段使用 String 类型
- **文件：** `app/models.py` 多处
- **问题：** 大量日期字段（如 `hire_date`, `birth_date`, `contract_start_date` 等）使用 `String(15)` 而非 `Date` 类型，导致：
  - 无法进行日期范围查询和排序
  - 无法利用数据库的日期函数
  - 数据格式不统一
- **风险等级：** 🟡 中

### 3.3 缺少数据库索引
- **文件：** `app/models.py`
- **问题：** 频繁查询的字段缺少索引：
  - `ComputerInfo.computer_name` — 搜索和导入时用于去重
  - `ComputerInfo.dept_code` — 部门权限过滤
  - `IndustrialComputer.device_name` — 导入去重
  - `Personnel.emp_id` — 频繁查询
  - `Personnel.emp_status` — Dashboard 统计
  - `Personnel.dept_code` — 部门权限过滤
  - `LoginLog.login_time` — 日志查询排序
  - `OperationLog.created_at` — 日志查询排序
- **风险等级：** 🟡 中（性能问题）

### 3.4 SysUserRole.user_id 没有外键约束
- **文件：** `app/models.py:334`
- **问题：** `user_id = db.Column(db.Integer, nullable=False)` 没有设置外键 `db.ForeignKey('user.id')`，无法保证数据完整性。
- **风险等级：** 🟡 中

---

## 四、前端用户体验问题 🟠

### 4.1 人员管理页面（推测）
- **问题：** `PersonnelForm` 包含 80+ 个字段，全部在一个表单中展示，用户体验极差。
- **建议：** 分步骤/分 Tab 展示，如：基本信息、部门信息、职位信息、合同信息等。

### 4.2 搜索功能缺少高级搜索
- **问题：** 办公电脑和工控机页面只有单一搜索框，无法按特定字段筛选。对于大量数据，单一搜索效率低下。
- **建议：** 增加按部门、操作系统、状态等字段的高级筛选。

### 4.3 分页每页数量固定
- **文件：** `app/routes/assets.py:57, 92`
- **问题：** 每页固定 50 条，用户无法调整。在数据量大时可能影响加载速度，在数据量小时频繁翻页。
- **建议：** 提供每页显示数量选择器（如 20/50/100/200）。

### 4.4 导入数据只跳过不更新
- **文件：** `app/routes/assets.py:410-413`, `689-692`
- **问题：** 导入时遇到已存在的记录直接跳过，没有提供"更新已有记录"的选项。用户需要先删除旧数据再导入，操作不便。
- **建议：** 提供"跳过/覆盖/更新"三种模式选择。

### 4.5 导入错误反馈不足
- **文件：** `app/routes/assets.py:446`
- **问题：** `errors[:10]` 只返回前 10 条错误，如果大量数据导入失败，用户无法看到完整错误信息。
- **建议：** 提供完整错误日志下载功能。

### 4.6 缺少批量操作
- **问题：** 办公电脑和工控机列表页面没有批量删除、批量编辑功能。对于大量数据的管理，操作效率低下。

### 4.7 缺少数据可视化
- **问题：** Dashboard 只显示三个数字（电脑数量、工控机数量、人员数量），缺少图表和趋势分析，信息展示不够直观。

### 4.8 缺少操作确认和反馈
- **问题：** 部分关键操作（如删除用户）的前端确认机制不确定是否完善。

---

## 五、代码质量问题 🟡

### 5.1 路由文件过大
- **文件：** `app/routes/assets.py` (729行)
- **问题：** 单个路由文件包含导入/导出/CRUD等大量逻辑，应拆分为更小的模块。

### 5.2 重复代码
- **文件：** `app/routes/assets.py`
- **问题：** 
  - 办公电脑和工控机的导入/导出逻辑高度重复
  - 样式设置代码（header_font, header_fill, thin_border 等）在多处重复
- **建议：** 提取为公共工具函数。

### 5.3 内存缓存方案不适用于生产环境
- **文件：** `app/routes/assets.py:18-19`, `app/routes/auth.py:13-19`
- **问题：** 项目中多处使用基于内存的缓存/计数器，在以下场景下失效：
  - 多 worker 进程部署
  - 容器化部署（容器重启后数据丢失）
  - 水平扩展
- **建议：** 生产环境使用 Redis。

### 5.4 get_user_permissions() 返回空字典给 admin
- **文件：** `app/decorators.py:96-97`
- **问题：** admin 用户返回空字典 `{}`，如果前端模板根据这个字典判断是否显示按钮，admin 用户可能看不到操作按钮。需要前端配合使用 `role == 'admin'` 判断。

### 5.5 缺少统一的错误处理
- **问题：** 虽然有 `app/templates/errors/404.html` 等模板，但路由代码中缺少统一的异常处理装饰器，部分异常可能泄露技术细节。

### 5.6 表单验证不完整
- **文件：** `app/forms.py`
- **问题：** `PersonnelForm` 所有字段都是可选的 `StringField`，没有验证逻辑。保存空数据不会有任何提示。

---

## 六、Docker/部署问题 🟡

### 6.1 默认数据库凭据
- **文件：** `config.py:10-14`
- **问题：** 默认使用 `nocobase/nocobase` 作为数据库凭据，且端口为非标准的 3307。虽然支持环境变量覆盖，但默认值不安全。

### 6.2 环境变量检查不足
- **文件：** `config.py:4-7`
- **问题：** SECRET_KEY 未设置时只发出 warning，程序仍然正常启动。生产环境应直接拒绝启动。

---

## 七、兼容性问题 🟡

### 7.1 MySQL 版本兼容性
- **问题：** SQLALCHEMY_ENGINE_OPTIONS 中的连接参数 `read_timeout`, `write_timeout` 是 PyMySQL 特定参数，如果切换到 mysqlclient 驱动会报错。

### 7.2 openpyxl 版本依赖
- **文件：** `app/routes/assets.py:8-10`
- **问题：** 项目重度依赖 openpyxl 进行 Excel 处理，但 `requirements.txt` 中可能未锁定版本，不同版本的 API 可能有差异。

---

## 八、功能完整性问题 🟡

### 8.1 注册功能已关闭但代码保留
- **文件：** `app/routes/auth.py:24-28`
- **问题：** 注册路由仍然存在，只是 redirect 到登录页。`RegistrationForm` 和相关的验证逻辑仍然保留，增加代码维护成本。

### 8.2 AI 助手功能依赖本地 Ollama
- **文件：** `config.py:39-41`
- **问题：** 默认配置使用本地 Ollama (`http://localhost:11434/v1`)，如果未安装 Ollama，AI 功能完全不可用，但前端可能仍有入口。

### 8.3 task_queue.py 功能不确定
- **文件：** `app/task_queue.py`
- **问题：** 存在任务队列模块，但不清楚是否正确集成和使用。

### 8.4 db_monitor.py 功能不确定
- **文件：** `app/db_monitor.py`
- **问题：** 存在数据库监控模块，但不确定是否正常工作。

---

## 九、问题汇总统计

| 严重等级 | 数量 | 说明 |
|---------|------|------|
| 🔴 严重 | 5 | 必须修复，涉及安全漏洞和数据完整性 |
| 🟠 高 | 5 | 应尽快修复，涉及权限和功能缺陷 |
| 🟡 中 | 11 | 建议修复，涉及性能和代码质量 |
| 🟢 低 | 4 | 可选修复，涉及最佳实践 |

---

## 十、优先修复建议

### 第一优先级（立即修复）
1. **删除用户时处理关联数据** — 设置级联删除或软删除
2. **统一密码策略** — 所有入口（注册、管理员创建、重置）使用相同规则
3. **Dashboard 缓存改为按用户/角色隔离** — 或移除缓存，避免数据权限泄露
4. **工控机页面添加部门权限过滤** — 与办公电脑保持一致
5. **SECRET_KEY 强制要求环境变量** — 生产环境未设置时拒绝启动

### 第二优先级（尽快修复）
6. **导出功能添加权限控制** — 使用 `@permission_required` 装饰器
7. **登录失败锁定改用 Redis** — 支持多进程部署
8. **修复开放重定向漏洞** — 排除 `//` 开头的路径
9. **添加数据库索引** — 优化高频查询性能
10. **SysUserRole.user_id 添加外键约束**

### 第三优先级（版本迭代）
11. 人员表单分步展示优化
12. 增加高级搜索功能
13. 导入功能增加"更新已有记录"选项
14. 代码去重和模块拆分
15. 数据可视化 Dashboard 增强