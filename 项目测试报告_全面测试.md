# 资产管理系统 - 全面测试报告

**测试日期**: 2026年5月9日  
**测试范围**: 全模块功能测试、前后端代码审查、安全性检查、用户体验评估  
**测试方法**: 代码审查 + 数据库验证 + 运行时测试

---

## 🔴 严重问题 (Critical) - 导致功能完全不可用

### C1. 安全模块模型与数据库表严重不匹配
**影响模块**: 涉密人员、涉密介质、安全区域、电子文件、纸质文件  
**问题描述**: SQLAlchemy模型定义缺少大量MySQL表中实际存在的列，导致通过ORM创建/查询记录时数据丢失或报错

| 模型 | 模型中定义的列 | MySQL实际列 | 缺失的列 |
|------|--------------|------------|---------|
| ClassifiedPersonnel | 11列 | 15列 | `position`, `classification_level`, `training_record`, `agreement_sign_date` |
| ClassifiedMedia | 12列 | 17列 | `media_id`, `brand_model`, `serial_no`, `classification`, `custodian_id`, `custodian_name` |
| SecurityZone | 12列 | 15列 | `zone_id`, `zone_type`, `manager_id`, `manager_name` |
| ElectronicDocument | 12列 | 19列 | `doc_id`, `classification`, `file_format`, `drafter_id`, `drafter_name`, `draft_dept`, `storage_path`, `custodian_id`, `custodian_name` |
| PaperDocument | 13列 | 21列 | `doc_id`, `classification`, `copies`, `pages`, `drafter_id`, `drafter_name`, `holder_id`, `holder_name`, `custodian_id`, `custodian_name` |

**严重性**: 🔴 严重 - 通过Web界面新增记录时，大量字段无法保存到数据库

### C2. 安全模块 `dept_id` 类型不匹配
**影响模块**: 所有5个安全子模块  
**问题描述**: MySQL数据库中 `dept_id` 是 `int` 类型，但SQLAlchemy模型定义为 `db.String(20)`。这导致：
- 通过ORM插入字符串格式的dept_id（如'D001'）会报错 `Incorrect integer value`
- 通过原始SQL插入的int型dept_id，ORM读取后返回字符串，可能导致关联查询失败

### C3. 涉密人员路由代码引用不存在的模型属性
**文件**: `app/routes/security.py`  
**问题描述**: 路由代码中引用了模型中未定义的属性，这些属性在MySQL表中也不存在：
- `person.agreement_type` → 模型中有此字段 ✓
- `person.signing_date` → 模型中有，但MySQL表中为 `agreement_sign_date`（varchar），模型中也定义了`signing_date`，存在歧义
- `person.expiration_date` → 模型中有此字段 ✓
- 模板中引用 `person.position`、`person.classification_level`、`person.training_record` → 模型中未定义 ❌

### C4. 安全模块新增/编辑功能可能崩溃
**影响模块**: 所有5个安全子模块的add/edit操作  
**问题描述**: 路由代码使用 `setattr(item, field, value)` 动态设置属性。由于模型定义缺少列，尝试设置不存在的属性时：
- 如果属性不在模型列中，SQLAlchemy不会将其包含在INSERT/UPDATE语句中 → 数据静默丢失
- 部分字段可能触发 `AttributeError`

---

## 🟠 重要问题 (Major) - 功能部分受损或数据不一致

### M1. SECRET_KEY 未设置
**文件**: `config.py`  
**问题描述**: `SECRET_KEY` 从环境变量读取，未设置时使用默认值并发出警告。生产环境中这会导致：
- Session可被伪造
- CSRF保护失效
- Flask-Login的认证token不安全

### M2. 涉密人员"签订日期"字段歧义
**问题描述**: MySQL表中同时存在：
- `agreement_sign_date` (varchar(30)) - 原始设计中的协议签订日期
- `signing_date` (varchar(20)) - 后来添加的签订日期
两个字段功能重叠，容易造成数据不一致。模型中只定义了 `signing_date`。

### M3. 安全模块created_at字段无默认值
**问题描述**: MySQL表中 `created_at` 定义为 `NOT NULL` 但没有DEFAULT值。通过ORM插入记录时如果忘记设置 `created_at`，会导致 `OperationalError: Field 'created_at' doesn't have a default value`。

### M4. 涉密介质/media_number vs media_id 字段冗余
**问题描述**: MySQL表中同时存在 `media_id` 和 `media_number` 两个字段，功能重叠。模型只定义了 `media_number`。

### M5. 安全区域/zone_id vs zone_code 字段冗余  
**问题描述**: MySQL表中同时存在 `zone_id` 和 `zone_code`，功能重叠。模型只定义了 `zone_code`。

### M6. 电子文档/doc_id vs doc_number 字段冗余
**问题描述**: MySQL表中同时存在 `doc_id` 和 `doc_number`。模型只定义了 `doc_number`。

### M7. 纸质文档/doc_id vs doc_number 字段冗余
**问题描述**: 同M6。

### M8. 安全模块"分类/密级"字段命名不一致
**问题描述**: 
- 涉密人员: MySQL有 `classification_level`，模型未定义
- 涉密介质: MySQL有 `classification`，模型未定义
- 安全区域: 模型有 `zone_level`，MySQL也有 ✓
- 电子文档: MySQL有 `classification` 和 `doc_level`，模型只定义了 `doc_level`
- 纸质文档: MySQL有 `classification` 和 `doc_level`，模型只定义了 `doc_level`

### M9. 涉密介质/电子文档/纸质文档的"保管人"字段冗余
**问题描述**: MySQL表中同时存在：
- `custodian_id`/`custodian_name` - 保管人
- `responsible_name`/`responsible_emp_id` - 负责人
模型只定义了 `responsible_*` 字段，`custodian_*` 完全丢失。

### M10. 安全模块"签发人/起草人"字段丢失
**问题描述**: 
- 电子文档MySQL有 `drafter_id`/`drafter_name`/`draft_dept` - 模型未定义
- 纸质文档MySQL有 `drafter_id`/`drafter_name` - 模型未定义
- 纸质文档MySQL有 `holder_id`/`holder_name`（持有人）- 模型未定义

---

## 🟡 一般问题 (Minor) - 体验不佳或潜在风险

### m1. 默认管理员密码不安全
**文件**: `app/routes/auth.py`  
**问题描述**: 新注册用户或重置密码时可能使用简单默认密码（如 `123456`）。虽然有重置密码功能，但缺少强制密码复杂度校验。

### m2. 涉密介质"品牌型号"和"序列号"字段在模型中缺失
**问题描述**: MySQL中有 `brand_model` 和 `serial_no`，是重要的资产追踪信息，但模型中未定义，无法通过Web界面管理。

### m3. 安全区域"类型"字段在模型中缺失
**问题描述**: MySQL有 `zone_type`（如机房、档案室、实验室），模型未定义。

### m4. 安全区域"管理人"字段在模型中缺失
**问题描述**: MySQL有 `manager_id`/`manager_name`，与 `responsible_*` 不同（管理人是日常管理责任，负责人是安全责任），模型未定义。

### m5. 电子文档"文件格式"和"存储路径"字段在模型中部分缺失
**问题描述**: MySQL有 `file_format` 和 `storage_path`，模型只定义了 `file_path`（可能对应 `storage_path`），`file_format` 丢失。

### m6. 纸质文档"份数/页数"字段在模型中缺失
**问题描述**: MySQL有 `copies`（份数）和 `pages`（页数），模型只定义了 `quantity`（数量），信息不完整。

### m7. 中文编码显示问题
**问题描述**: 命令行运行Python脚本时，中文字符显示为乱码（`???`），说明Windows CMD的编码与Python输出不兼容。不影响Web功能，但影响调试体验。

### m8. 日期字段类型不一致
**问题描述**: 
- 模型中 `signing_date`/`expiration_date` 定义为 `db.Date`
- MySQL实际为 `varchar(20)`
- `agreement_sign_date` 也是 `varchar(30)`
这种类型不匹配可能导致日期比较/排序功能异常。

---

## 🔵 用户体验问题 (UX)

### UX1. 安全模块数据展示不完整
**问题描述**: 由于模型缺少大量字段，Web界面只能显示部分数据。例如：
- 涉密人员页面无法显示"职位"、"密级"、"培训记录"
- 涉密介质页面无法显示"品牌型号"、"序列号"、"密级"
- 电子文档页面无法显示"文件格式"、"起草人"、"存储路径"

### UX2. 安全模块表单字段不完整
**问题描述**: 新增/编辑表单只能填写模型中定义的少量字段，大量有实际业务价值的字段无法录入。

### UX3. 安全模块搜索功能可能受限
**问题描述**: 无法按缺失的字段（如密级、分类、品牌）进行搜索过滤。

---

## 📊 各模块测试状态

| 模块 | 列表显示 | 新增 | 编辑 | 删除 | 搜索 | 状态 |
|------|---------|------|------|------|------|------|
| 登录/注册 | - | ✅ | - | - | - | 基本正常 |
| 仪表盘 | ✅ | - | - | - | - | 基本正常 |
| 办公电脑 | ✅ | ⚠️ | ⚠️ | ✅ | ✅ | 部分字段问题 |
| 工业电脑 | ✅ | ⚠️ | ⚠️ | ✅ | ✅ | 部分字段问题 |
| 人员管理 | ✅ | ✅ | ✅ | ✅ | ✅ | 基本正常 |
| 涉密人员 | ⚠️ | ❌ | ❌ | ✅ | ⚠️ | **严重问题** |
| 涉密介质 | ⚠️ | ❌ | ❌ | ✅ | ⚠️ | **严重问题** |
| 安全区域 | ⚠️ | ❌ | ❌ | ✅ | ⚠️ | **严重问题** |
| 电子文件 | ⚠️ | ❌ | ❌ | ✅ | ⚠️ | **严重问题** |
| 纸质文件 | ⚠️ | ❌ | ❌ | ✅ | ⚠️ | **严重问题** |
| 权限矩阵 | ✅ | ✅ | ✅ | ✅ | - | 基本正常 |
| 审计任务 | ✅ | ✅ | ✅ | ✅ | ✅ | 基本正常 |
| AI助手 | ✅ | - | - | - | ✅ | 基本正常 |
| 部门管理 | ✅ | ✅ | ✅ | ✅ | - | 基本正常 |
| 用户管理 | ✅ | ✅ | ✅ | ✅ | ✅ | 基本正常 |

✅ = 正常 | ⚠️ = 部分功能异常 | ❌ = 功能不可用

---

## 🔧 修复建议优先级

### P0 - 立即修复（影响核心功能）

1. **修复安全模块5个模型定义** - 补充所有MySQL中存在但模型中缺失的列
2. **修复dept_id类型** - 将模型中 `dept_id` 从 String 改为 Integer
3. **修复路由代码中的字段引用** - 确保路由和模板使用正确的字段名

### P1 - 尽快修复（影响数据完整性）

4. **清理冗余字段** - 统一 media_id/media_number, zone_id/zone_code, doc_id/doc_number 等
5. **统一密级字段命名** - 将 classification_level/classification/doc_level/zone_level 统一
6. **统一负责人字段** - 将 custodian/responsible/manager/holder 等关系明确化
7. **修复日期字段类型** - 将 varchar 日期改为 proper date 类型或统一为字符串

### P2 - 计划修复（提升安全性和体验）

8. **设置生产环境 SECRET_KEY**
9. **添加密码复杂度校验**
10. **为 created_at 添加默认值**
11. **补充前端表单缺失字段**
12. **添加数据验证**

---

## 问题统计

| 严重级别 | 数量 |
|---------|------|
| 🔴 严重 (Critical) | 4 |
| 🟠 重要 (Major) | 10 |
| 🟡 一般 (Minor) | 8 |
| 🔵 体验 (UX) | 3 |
| **总计** | **25** |

---

## 总结

本项目最大的问题集中在**安全模块**（涉密人员、涉密介质、安全区域、电子文件、纸质文件），根本原因是 **SQLAlchemy模型定义与MySQL实际表结构严重不同步**。MySQL表经过多次迭代增加了很多字段，但模型定义没有同步更新，导致：

1. **大量字段无法通过Web界面管理**（模型未定义的列在ORM操作中会被忽略）
2. **数据类型不匹配**（如 dept_id 的 int vs String）
3. **字段命名冗余和歧义**（如 media_id/media_number, signing_date/agreement_sign_date）

其他模块（登录注册、人员管理、资产管理、审计、AI助手、权限管理）基本功能正常。

**建议**: 优先修复5个安全模块的模型定义，使其与MySQL表完全对齐，然后同步更新路由和模板代码。
