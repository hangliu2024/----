# 代码审计报告

## 一、严重问题 🔴

### 1. CSRF保护已启用但AJAX请求未携带Token
**位置**: `app/__init__.py:20` + 所有AJAX POST路由
**问题**: `csrf = CSRFProtect(app)` 已全局启用，但 `audit.py`、`security.py`、`personnel.py` 中的大量 AJAX POST 请求（如 `/permissions/update`、`/users/toggle`、`/person_permission/update` 等约 15 个端点）均未在 fetch 请求中携带 CSRF token。
**影响**: 正式环境会直接返回 400 Bad Request，所有 AJAX 功能瘫痪。

### 2. 错误页面模板缺失
**位置**: `app/__init__.py:46-56`
**问题**: 引用了 `render_template('errors/404.html')`、`errors/500.html`、`errors/403.html`，但 `/app/templates/errors/` 目录不存在。
**影响**: 触发404/500/403时抛出 `TemplateNotFound` 异常，用户看到原始错误而不是友好页面。

### 3. run.py 硬编码 debug=True，覆盖环境变量控制
**位置**: `run.py:4`
**问题**: `app.run(debug=True)` 直接覆盖了 `app/__init__.py` 中 `app.debug = os.environ.get('FLASK_DEBUG', '0') == '1'` 的逻辑，使 debug 模式无法通过环境变量控制关闭。
**影响**: 生产环境可能误开 debug 模式，暴露敏感信息和 Werkzeug debugger。

### 4. 硬编码默认 API Key
**位置**: `app/routes/ai_assistant.py:24-30`
**问题**: 当未配置 AI_API_KEYS 时，默认使用 `asset-ai-api-key-2024`，且这个 Key 在项目代码和对话中多次暴露。
**影响**: API 访问形同虚设，任何人知道这个 Key 就能直接SQL查询数据库。

### 5. AI助手允许直接执行任意SQL
**位置**: `app/routes/ai_assistant.py` - `/api/v1/sql` 端点
**问题**: 外部 API 可以提交任意 SQL 语句直接执行。虽然有 API Key 保护，但 Key 泄露后攻击者可执行 `DROP TABLE` 等破坏性操作。
**影响**: 数据库完全暴露。

### 6. person_permission_save 接口存在O(N²)性能问题
**位置**: `app/routes/security.py:318-382`
**问题**: 对每个员工 × 每个系统（约20个系统），都执行一次 `filter_by(emp_id, system_name).first()` 查询。如果保存 100 个员工就是 2000 次数据库查询。
**影响**: 接口超时、数据库压力过大。

### 7. 导入功能逐条 INSERT，93K数据导入会极慢
**位置**: `app/routes/personnel.py:673-744`、`assets.py:310+`
**问题**: Excel 导入时逐条 `db.session.add()` + 逐条检查是否重复，大文件导入效率极低。
**影响**: 导入稍大文件时页面卡死。

---

## 二、中等问题 🟡

### 8. 权限矩阵页面整表加载
**位置**: `app/routes/security.py:193-220`
**问题**: `SysPermission.query.all()` 加载全部角色×模块权限到内存，7角色×25模块=175条，但模块数量会增长。
**影响**: 未来模块增长后页面变慢。

### 9. 员工权限矩阵页多次COUNT查询
**位置**: `app/routes/security.py:231-287`
**问题**: `permission_list()` 中先 `total_emp = employees_query.count()`，然后又 `employees_query.offset().limit().all()`，COUNT 和主查询之间数据可能变化导致分页不准确。
**影响**: 分页在极少数情况下可能错位。

### 10. 人员编辑页 80+ 字段逐行赋值
**位置**: `app/routes/personnel.py:192-312`
**问题**: `update_personnel()` 中对 80+ 字段逐行写 `person.xxx = form.xxx.data`，代码极度冗长。
**影响**: 维护困难，容易遗漏新增字段。

### 11. 稽查管理只用硬编码角色判断
**位置**: `app/routes/audit.py:20,65,78`等多个位置
**问题**: 新 RBAC 系统建好后，稽查模块仍用 `current_user.role != 'admin'` 做权限判断，未接入 `permission_required` 装饰器。
**影响**: 角色权限矩阵配置对这个模块无效。

### 12. 表单导入无大小限制校验
**位置**: `app/routes/personnel.py:660`、`assets.py`
**问题**: 虽然 `app.config['MAX_CONTENT_LENGTH'] = 16MB`，但 Excel 解析时 `openpyxl.load_workbook(file)` 会将整个文件加载到内存，大文件会导致 OOM。
**影响**: 攻击者上传超大文件耗尽内存。

### 13. AI API 调用未做超时和异常充分处理
**位置**: `app/routes/ai_assistant.py:50-95`
**问题**: LLM API 调用设置了 timeout=120，但 `requests.post()` 在某些网络条件下可能永久挂起不会真正超时。此外多模型切换时部分配置字段可能为空导致程序崩溃。
**影响**: 用户等待过久或服务500。

---

## 三、轻微问题 🟢

### 14. 全局级别 `g` 变量使用不一致
**位置**: `app/decorators.py:124-130` vs 之前版本
**问题**: `get_user_permissions` 中用 `setattr(g, cache_key, perms)` 存储缓存，但 `g` 对象在同一个请求内有效，而代码又用 `current_user._permission_cache` 做双重缓存，逻辑混乱。
**影响**: 缓存可能不生效。

### 15. docker-compose 中 MySQL 镜像源可能不可用
**位置**: `docker-compose.yml:28`
**问题**: 使用了 `swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/library/mysql:8.0`，这是华为云内部镜像，外部用户无法拉取。
**影响**: 其他人无法通过 docker-compose 启动。

### 16. docker-compose 中 SECRET_KEY 硬编码
**位置**: `docker-compose.yml:13`
**问题**: `SECRET_KEY=your-secret-key-change-in-production` 是占位符，容易被疏忽而部署到生产环境。
**影响**: Session 可被伪造。

### 17. 大量调试脚本文件未清理
**位置**: 项目根目录
**问题**: 超过 30 个一次性脚本（如 `check_data.py`、`sync_employees_to_matrix.py`、`batch_update_dept.py` 等）散落在项目根目录，其中部分文件硬编码了数据库密码 `nocobase:nocobase`。
**影响**: 代码库混乱，敏感信息泄露风险。

### 18. 导出功能全表查询无分页
**位置**: `app/routes/personnel.py:577`、`assets.py`
**问题**: 导出 Excel 时 `query.all()` 一次性加载全部 93K 条记录。
**影响**: 内存爆增，服务可能 OOM。

### 19. 部门管理页中的递归查询可能造成 N+1
**位置**: `app/decorators.py:97-111`
**问题**: `get_department_codes_recursive()` 对每个部门递归查询子部门，每个子部门一次 DB 查询。深层嵌套时查询次数爆炸。
**影响**: 深层部门结构下性能下降。

### 20. 单点登录的 `check_session_valid` 在 before_request 中每次都查询 User
**位置**: `app/__init__.py:101-113`
**问题**: 每个请求都调用 `check_session_valid()`，间接触发 SQLAlchemy 加载 User 对象。
**影响**: 轻微性能损耗。

### 21. 登录频次限制使用内存，服务重启后丢失
**位置**: `app/routes/auth.py:13-18`
**问题**: `_login_failures` 和 `_registration_attempts` 都存储在内存字典中，服务重启或水平扩展后所有限制记录丢失。
**影响**: 攻击者可通过重启服务绕过登录限制。

### 22. 权限矩阵配置页无CSRF豁免但使用JSON提交
**位置**: `app/routes/security.py:237`
**问题**: `permissions_batch_save` 端点是 JSON POST（Content-Type: application/json），而 Flask-WTF CSRF 默认只检查 form 类型的请求。
**影响**: 可能工作正常，但依赖 Flask-WTF 的默认行为不稳定。

---

## 四、建议修复优先级

| 优先级 | 编号 | 问题 | 预估工时 |
|--------|------|------|----------|
| P0 | 1 | CSRF AJAX 兼容 | 1h |
| P0 | 2 | 错误页面模板 | 15min |
| P0 | 5 | AI API 直接SQL限制 | 30min |
| P0 | 4 | 默认API Key移除 | 10min |
| P0 | 3 | debug模式控制 | 5min |
| P1 | 6 | person_permission_save 性能 | 1h |
| P1 | 11 | 稽查模块接入RBAC | 30min |
| P1 | 12 | 导入文件大小二次校验 | 15min |
| P1 | 17 | 清理历史脚本 | 1h |
| P2 | 7-10,13-22 | 其余问题 | 合计约4h |

---

**审计时间**: 2026-05-06
**审计范围**: 全部代码文件（路由、模型、模板、配置、部署）
